#!/bin/bash
# Ubuntu 20.04 (Focal) specific versions
# Older system may need older package versions

export VERSION_NEOVIM="v0.9.5"          # Older neovim for compatibility

export VERSION_NODE="18"                 # LTS version
